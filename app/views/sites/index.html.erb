<header>
  <h1>Sites</h1>

  <ul class="actions">
    <li><%= link_to "New Site", new_site_path,
      class: "btn new", id: "new-site" %></li>
  </ul>
</header>

<div class="banner">
  Trying to track a URL your users depend on? Try the <a href="http://www.spiceworks.com/it-troubleshooting/">Spiceworks Connectivity Dashboard</a>.
</div>

<div class="sites">
  <% @sites.each do |site| %>
    <h2 class="<%= site_css_class(site) %>"><%= link_to site.name, site %></h2>
    <p><%= site.url %></p>
  <% end %>
</div>
